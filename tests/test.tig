let

	type Cat = {name: string, age: int}


	var bar := 3
	var barstr : string := "3"

	var cat : Cat := Cat{name="Thomas", age=2}

	type intArray = array of int
	var ints := intArray[3] of 1

	function foo(a: int, b: int) =
		let
			var y := a + b
		in
			for a := 0 to 12 - 2 do cat.age := a;
			while cat.age < 100 do
				(cat.age := cat.age * 2;
				if cat.age = 97 then break);
			y
		end

in
	foo(2, cats[0].age[0].bar.hello[1][2])

end