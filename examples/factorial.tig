let
    function factorial(n: int): int =
        let
            function fact(acc: int, n: int): int =
                if n >= 1 then fact(acc * n, n - 1) else acc
        in
            fact(1, n)
        end
in
    factorial(7)
end